{
  "name": "mental-poker-precompile-contract",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "run scripts/deploy.ts",
    "hardhat:dev": "yarn hardhat --network localhost",
    "hardhat:test": "yarn hardhat --network testnet",
    "compile": "yarn hardhat compile",
    "start": "yarn hardhat node",
    "test": "yarn hardhat test",
    "deploy:dev": "yarn hardhat:dev deploy",
    "deploy:test": "yarn hardhat:test deploy",
    "deploy-test:dev": "yarn hardhat:dev deploy-test",
    "deploy-test:test": "yarn hardhat:test deploy-test",
    "mt:dev": "yarn hardhat --network localhost mt",
    "mt:test": "yarn hardhat --network testnet mt"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "@nomiclabs/hardhat-ganache": "^2.0.1",
    "hardhat": "^2.13.0",
    "hardhat-abi-exporter": "^2.10.1",
    "hardhat-contract-sizer": "^2.8.0",
    "hardhat-deploy": "^0.11.25",
    "hardhat-deploy-ethers": "^0.3.0-beta.13",
    "hardhat-diamond-abi": "^3.0.1",
    "hardhat-gas-reporter": "^1.0.9",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  }
}
